name = "Fernando"

puts "Valor de name: #{name}"

puts "Llamando al método upcase en name : #{name.upcase}"

puts "Valor de name despues de pasarlo por gsub: #{name}"

puts "Llamando al método upcase! en name : #{name.upcase!}"

puts "Valor de name después de pasarlo por upcase!: #{name}"
